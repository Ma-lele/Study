using System;
using System.Collections.Generic;
using System.Text;

namespace Common.Sqlsugar
{
    public  class SqlsugarDB
    {
        //public void db(IConfiguration configuration)
        //{
        //    //configuration.GetSection("DBConfig").Get<DbConfig>();

        //    //SqlSugarClient db = new SqlSugarClient(new ConnectionConfig()
        //    //{
        //    //    ConnectionString = configuration.GetSection("DBConfig").ToString(),//连接符字串
        //    //    DbType = (DbType)Convert.ToInt32(configuration.GetSection("DBConfig")), //数据库类型
        //    //    IsAutoCloseConnection = true //不设成true要手动close
        //    //});
        //}
    }
}
